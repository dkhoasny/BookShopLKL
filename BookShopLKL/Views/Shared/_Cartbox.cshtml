@if (@ViewBag.cartBox != null)
{
    foreach (var item in @ViewBag.cartBox)
    {
        <li>
            <a class="aa-cartbox-img" href="#"><img src="@item.Product.PicturePath" alt="@item.Product.Name"></a>
            <div class="aa-cartbox-info">
                <h4><a href="#">@item.Product.Name</a></h4>
                <p>@item.Quantity x @item.UnitPrice</p>
            </div>
            <a class="aa-remove-product" href="@Url.Action("Remove", "MyCart", new { id = item.ProductID })"><span class="fa fa-times"></span></a>
        </li>
    }
}

<li>
    <span class="aa-cartbox-total-title">
        Total
    </span>
    <span class="aa-cartbox-total-price">
        @(ViewBag.Total ?? 0) <!-- Tránh lỗi null -->
    </span>
</li>
